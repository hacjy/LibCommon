<?xml version="1.0" encoding="utf-8"?>
<!-- 加载视图 -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/loading_view_background"
    >

    <ProgressBar
        android:id="@+id/pb_loading_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateDrawable="@drawable/loading_view_progressbar"
        />
    <TextView
        android:id="@+id/tv_loading_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/load_start_view_text_color"
        android:paddingTop="6dp"
        android:text="@string/txt_load_start_view"/>

</LinearLayout>